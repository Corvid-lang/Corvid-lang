# Replay determinism rate — published results

> Auto-generated by `benches/moat/replay_determinism/runner/run.py`. Do not hand-edit. Re-run the runner after adding or modifying stack runners.

## Headline numbers

- Corvid (`cargo run -p refund_bot_demo`): **190/190 byte-identical pairs** (rate = 1.000, N = 20)
- Python (LangChain + LangSmith): **bounty-open** (no `_summary.json`)
- TypeScript (Vercel AI SDK + OTEL): **bounty-open** (no `_summary.json`)

## Per-stack verdicts

| Stack | N | Identical pairs | Total pairs | Rate | Status |
|-------|---|-----------------|-------------|------|--------|
| `corvid` | 20 | 190 | 190 | 1.000 | ran |
| `python` | — | — | — | — | bounty-open |
| `typescript` | — | — | — | — | bounty-open |

## Methodology

See `benches/moat/replay_determinism/README.md` for the run layout, normalization rules, and bounty window. The runner is non-judgmental — it reads each stack's `_summary.json` and renders the headline. Each stack's runner is responsible for running N times, applying its documented normalization, and writing the summary.
