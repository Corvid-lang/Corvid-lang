id = "21-tokens-budget-over"
title = "Tool token cost exceeds caller's @tokens budget ceiling"
bug_class = "budget-overrun"
description = """
The tokens dimension is a budget-style numeric — caller's @tokens
declaration is the ceiling and the tool's known token cost exceeds it.
"""

[expected]
corvid = "rejected"
python = "accepted"
typescript = "accepted"

corvid_guarantee_id = "budget.compile_time_ceiling"
python_baseline = "mypy --strict + pydantic"
typescript_baseline = "tsc --strict + zod"
